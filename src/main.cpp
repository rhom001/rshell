/** 
 * This is the main file that will be used to run the rshell.
**/

#include <iostream>
#include <string>

using namespace std;

int main()
{
    string input = "";
    if(input != "exit")
    {
        cout << "$";
        cin >> input;
    }		    
    return 0;
}
